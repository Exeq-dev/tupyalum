---
const { text } = Astro.props;
---

<header class="relative w-full bg-gray-200">
    <img src= alt="Encabezado" class="w-full h-auto object-cover" />
    <div class="absolute inset-0 flex items-center justify-center">
      <h1 class="text-4xl font-bold text-white bg-blue-900 p-4 rounded-lg shadow-lg">
        {text}
      </h1>
    </div>
  </header>